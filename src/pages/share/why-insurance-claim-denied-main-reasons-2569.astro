---
const title = "ซื้อประกันแล้วทำไมเคลมไม่ได้? สาเหตุหลักที่บริษัทปฏิเสธการจ่าย | พี่มิก ไทยประกัน";
const description = "ชำแหละสาเหตุที่ทำให้เคลมประกันไม่ได้ เช่น Waiting Period โรคที่เป็นมาก่อน ข้อยกเว้น เอกสารไม่ครบ และวิธีป้องกันก่อนซื้อ";
const siteUrl = "https://insurance-by-miixz.pages.dev";
const articleUrl = `${siteUrl}/blog/why-insurance-claim-denied-main-reasons-2569/`;
const shareUrl = `${siteUrl}/share/why-insurance-claim-denied-main-reasons-2569/`;
const imageUrl = `${siteUrl}/images/why-insurance-claim-denied-2569-cover.jpg`;
---

<!doctype html>
<html lang="th">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={articleUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="article" />
    <meta property="og:url" content={shareUrl} />
    <meta property="og:image" content={imageUrl} />
    <meta property="og:image:secure_url" content={imageUrl} />
    <meta property="og:image:type" content="image/jpeg" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={imageUrl} />
    <meta http-equiv="refresh" content={`0;url=${articleUrl}`} />
  </head>
  <body>
    <p><a href={articleUrl}>อ่านบทความต่อ</a></p>
  </body>
</html>
